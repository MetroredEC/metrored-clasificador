name            = "metrored-clasificador"
main            = "src/index.js"
compatibility_date  = "2024-09-23"
compatibility_flags = ["nodejs_compat"]

# ⚠️  Los secretos NO van aquí — se configuran con:
#     wrangler secret put ANTHROPIC_API_KEY
#     wrangler secret put GOOGLE_SERVICE_ACCOUNT
#
# Para PDFs grandes (>50 páginas) se recomienda el plan Workers Paid
# que elimina el límite de tiempo de CPU.
